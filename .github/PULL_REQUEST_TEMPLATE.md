## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `npx tsc --noEmit` passes
- [ ] `npx eslint src tests` passes with zero errors
- [ ] `npx vitest run` — all tests green, new behaviour has tests
- [ ] Docs updated if public API or config changed
